_schema-version: "3.2"
ID: notesapp
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: notesapp-destination-content
  type: com.sap.application.content
  requires:
  - name: notesapp-destination-service
    parameters:
      content-target: true
  - name: notesapp-repo-host
    parameters:
      service-key:
        name: notesapp-repo-host-key
  - name: notesapp-uaa
    parameters:
      service-key:
        name: notesapp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: notesapp_html_repo_host
          ServiceInstanceName: notesapp-repo-host
          ServiceKeyName: notesapp-repo-host-key
          sap.cloud.service: notesapp
        - Authentication: OAuth2UserTokenExchange
          Name: notesapp_uaa
          ServiceInstanceName: notesapp-xsuaa-service
          ServiceKeyName: notesapp-uaa-key
          sap.cloud.service: notesapp
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: notesapp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: notesapp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - notesapp.zip
      name: notesapp
      target-path: resources/
- name: notesapp
  type: html5
  path: .
  requires:
    - name: notesapp-repo-host
    - name: notesapp-repo-runtime 
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []

- name: notesapp-approuter
  type: approuter.nodejs
  path: approuter
  parameters:
    memory: 512M
    disk-quota: 512M
    routes:
      - route: https://ea7f4d11trial-notesapp.${default-domain}
      - route: https://subcriber-zft5b92r-notesapp.${default-domain}
    buildpacks:
      - nodejs_buildpack
  properties:
    TENANT_HOST_PATTERN: "^(.*)-notesapp.cfapps.us10-001.hana.ondemand.com"
  
  build-parameters:
      builder: custom
      commands:
        - npm install
  requires:
    - name: notesapp-uaa
    - name: notesapp-destination-service
    - name: notesapp-repo-runtime
    - name: saas-registry-notes
    - name: connectivity

resources:
- name: notesapp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: notesapp-destination-service
    service-plan: lite
- name: notesapp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: notesapp-xsuaa-service
    service-plan: application
- name: notesapp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: notesapp-repo-host
    service-plan: app-host

- name: notesapp-repo-host-key
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
    service-name: notesapp-repo-host
    service-key:
      name: notesapp-repo-host-key

- name: notesapp-repo-runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
    service-name: notesapp-repo-runtime

- name: connectivity
  type: org.cloudfoundry.managed-service
  parameters:
    service: connectivity
    service-name: connectivity-service
    service-plan: lite

- name: saas-registry-notes
  type: org.cloudfoundry.managed-service
  parameters:
    service: saas-registry
    service-plan: application
    config:
      xsappname: notesapp
      appName: notesapp
      displayName: Notes App
      description: Multitenant Notes application
      category: Custom Apps
      appUrls:
        onSubscription: https://ea7f4d11trial-notesapp.${default-domain}/callback/v1.0/tenants/{tenantId}
        getDependencies: https://ea7f4d11trial-notesapp.${default-domain}/callback/v1.0/dependencies
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
